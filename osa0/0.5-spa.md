# 0.5: Single Page App

```mermaid
sequenceDiagram
    participant Browser
    participant Server

    Browser->>Server: GET /exampleapp/spa
    Server-->>Browser: HTML + CSS + JS

    Browser->>Server: GET /data.json
    Server-->>Browser: [{ "content": "пример заметки", "date": "2023-1-1" }]

    Note right of Browser: JS показывает заметки на странице без перезагрузки
