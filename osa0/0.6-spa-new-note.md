# 0.6: New Note (SPA)

```mermaid
sequenceDiagram
    participant Browser
    participant Server

    Note over Browser: User types a new note and clicks Save
    Browser->>Server: POST /exampleapp/notes { "content": "new note" }
    Server-->>Browser: 201 Created { "content": "new note", "date": "2023-1-1" }

    Note right of Browser: JS adds the new note to the page without reloading
